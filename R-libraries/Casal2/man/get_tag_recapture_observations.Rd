% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_tag_recapture_observations.R
\name{get_tag_recapture_observations}
\alias{get_tag_recapture_observations}
\alias{get_tag_recapture_observations.casal2MPD}
\alias{get_tag_recapture_observations.list}
\alias{get_tag_recapture_observations.casal2TAB}
\title{get_tag_recapture_observations}
\usage{
get_tag_recapture_observations(model, ...)

\method{get_tag_recapture_observations}{casal2MPD}(model, reformat_labels = TRUE, ...)

\method{get_tag_recapture_observations}{list}(model, reformat_labels = TRUE, ...)

\method{get_tag_recapture_observations}{casal2TAB}(model, reformat_labels = TRUE, ...)
}
\arguments{
\item{model}{A \code{casal2MPD}, \code{casal2TAB}, or named \code{list} of
\code{casal2MPD} objects produced by \code{extract.mpd()} or
\code{extract.tabular()}.}

\item{\dots}{Further arguments (currently unused).}

\item{reformat_labels}{Logical. Reformat default Casal2 report labels
(default \code{TRUE}).}
}
\value{
For \code{casal2MPD} input: a wide data frame whose columns come
  directly from the Casal2 report plus \code{observation_label},
  \code{observation_type}, \code{likelihood}, and \code{par_set}.
  For \code{casal2TAB} input: a long-form data frame with columns
  \code{iteration}, \code{parameter}, \code{value}, \code{category},
  \code{observation_label}, \code{observation_type}, and
  \code{likelihood}.
  Returns \code{NULL} when no matching reports are found.
}
\description{
Extract tag-recapture observation data from a Casal2 model
  output into a tidy data frame. Handles both single-run and multi-run
  (\code{-r -i}) MPD output and named lists of multiple MPD objects.
  When different reports contain different column sets, only columns common
  to all are retained.
}
\author{
Casal2 Development Team
}
