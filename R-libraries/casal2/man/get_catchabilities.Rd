% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_catchabilities.R
\name{get_catchabilities}
\alias{get_catchabilities}
\alias{get_catchabilities.casal2MPD}
\alias{get_catchabilities.list}
\alias{get_catchabilities.casal2TAB}
\title{get_catchabilities}
\usage{
get_catchabilities(model, ...)

\method{get_catchabilities}{casal2MPD}(model, reformat_labels = TRUE, ...)

\method{get_catchabilities}{list}(model, reformat_labels = TRUE, ...)

\method{get_catchabilities}{casal2TAB}(model, reformat_labels = TRUE, ...)
}
\arguments{
\item{model}{A \code{casal2MPD}, \code{casal2TAB}, or named \code{list} of
\code{casal2MPD} objects produced by \code{extract.mpd()} or
\code{extract.tabular()}.}

\item{\dots}{Further arguments (currently unused).}

\item{reformat_labels}{Logical. Reformat default Casal2 report labels by
removing leading/trailing underscores (default \code{TRUE}).}
}
\value{
For \code{casal2MPD} input: a data frame with columns \code{par_set}
  (integer index of the parameter set/minimisation; 1 for single run, ordered
  factor for multi-run \code{-i} output), \code{label}, and
  \code{catchability}.
  For \code{casal2TAB} (MCMC) input: a data frame with columns
  \code{iteration} (integer MCMC posterior-sample index), \code{parameter}
  (catchability parameter name as emitted by Casal2, e.g.
  \code{"q[survey1]"}), \code{catchability}, and \code{label}.
  Returns \code{NULL} when no matching reports are found.
}
\description{
Extract catchability values from a Casal2 model output into a
  tidy data frame.  Handles both single-run (\code{-r}) and multi-run
  (\code{-r -i}) MPD output, tabular (\code{casal2TAB}) output, and
  named lists of multiple MPD objects.
}
\seealso{
\code{\link{has_catchabilities}}, \code{\link{reformat_default_labels}}
}
\author{
Casal2 Development Team
}
