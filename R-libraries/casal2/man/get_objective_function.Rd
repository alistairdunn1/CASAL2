% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_objective_function.R
\name{get_objective_function}
\alias{get_objective_function}
\alias{get_objective_function.casal2MPD}
\alias{get_objective_function.list}
\alias{get_objective_function.casal2TAB}
\title{get_objective_function}
\usage{
get_objective_function(model, ...)

\method{get_objective_function}{casal2MPD}(
  model,
  aggregate_obs = TRUE,
  reformat_labels = TRUE,
  ...
)

\method{get_objective_function}{list}(
  model,
  aggregate_obs = TRUE,
  reformat_labels = TRUE,
  ...
)

\method{get_objective_function}{casal2TAB}(
  model,
  aggregate_obs = TRUE,
  reformat_labels = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{casal2MPD}, \code{casal2TAB}, or named \code{list} of
\code{casal2MPD} objects.}

\item{\dots}{Further arguments (currently unused).}

\item{aggregate_obs}{Logical.  When \code{TRUE} (default) observation
components are aggregated over years; when \code{FALSE} the raw per-year
component values are returned.}

\item{reformat_labels}{Logical. Reformat default Casal2 report labels
(default \code{TRUE}).}
}
\value{
A data frame with columns \code{component}, \code{negative_loglik},
  and \code{par_set} (integer index of the parameter set/minimisation; 1 for
  a single run, ordered factor for multi-run \code{-i} output).  When
  \code{aggregate_obs = TRUE} (default) observation components are summed
  across years before returning.  Returns \code{NULL} when no matching report
  is found.
}
\description{
Extract and (optionally) aggregate objective function components
  from a Casal2 model output.  Handles single-run and multi-run MPD output,
  tabular (\code{casal2TAB}) output, and named lists of multiple MPD objects.
}
\author{
Casal2 Development Team
}
